name: taxlien_swipe_app
description: "TaxLien Swipe App - Deal Detective Standalone"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.1.1
  url_launcher: ^6.2.1
  path_provider: ^2.1.0
  share_plus: ^12.0.0
  go_router: ^13.2.0
  
  # UI
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  flutter_animate: ^4.2.0+1
  
  # Utils
  intl: ^0.19.0
  uuid: ^4.2.2
  json_annotation: ^4.8.1
  
  # Local Storage
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  assets:
    - .env